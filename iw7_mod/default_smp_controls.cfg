unbindall
unbindallaxis

// Movement
bind w	"+forward"
bind s	"+back"
bind a	"+moveleft"
bind d	"+moveright"

// Weapon Controls
bind SHIFT	"+breath_sprint"
bind MOUSE1	"+attack"
bind MOUSE2	"+toggleads_throw"
bind v	 	"+melee_zoom"
bind q	 	"+special"

// Action Slots
bind 1		"+actionslot 1" // SP: Alt Weapon MP: Scroll Up
bind 2		"+actionslot 2" // SP: KillStreak MP: Scroll down
bind 3		"+actionslot 3" // SP: Cycle Tactical Equipment MP: Taunt
bind 4		"+actionslot 4" // SP: Cycle Lethal Equipment MP: Activate Killstreak

bind 5		"+actionslot 5"  // MP only: Use Killstreak slot 3
bind 6		"+actionslot 6"  // MP only: Use Killstreak slot 4

// Weapon Slots
bind MWHEELUP		"+weapnext"
bind MWHEELDOWN		"+weapnext"

// Inventory
bind MOUSE3	"+frag"
bind g		"+frag"
bind e		"+smoke"

// Interaction
bind f		"+activate"
bind r		"+reload"
bind TAB	"togglescores"

// Stance
bind SPACE	"+gostand"
bind CTRL	"+stance"
// bind c		"" // TODO: Add dedicated togle "alt weapon mode"

// Mouse movement dvars
set sensitivity		"5"
set cl_freelook		"1"
set ui_mousePitch	"0"
set m_pitch			"0.022"
set m_filter		"0"
set cl_mouseAccel	"0"

// Menus
bind ESCAPE			"togglemenu"
bind ~				"toggleconsole"
bind `				"toggleconsole"

// MULTIPLAYER CONTROLS
bind t	"chatmodepublic"
bind y	"chatmodeteam"
bind z	"+talk"

// CLIENT ENVIRONMENT COMMANDS
bind F1		"vote yes"
bind F2		"vote no"
bind F5		"skip"
bind F12	"screenshotJPEG"